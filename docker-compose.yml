version: "3.9"

services:
  frontend:
    build: .
    ports:
      - "3000:80"
    depends_on:
      - db

  db:
    image: postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
    ports:
      - "5432:5432"
